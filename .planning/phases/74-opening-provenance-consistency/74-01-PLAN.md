# P74-01 Opening Provenance Consistency Plan

## Objective

Add a downstream coverage view that combines opening semantic, measurement, and host-wall provenance so reviewers can see which evidence surfaces are present or missing per opening.

## Context

- P71 writes `properties.opening_semantic`.
- P72 writes `properties.opening_measurement`.
- P73 writes `properties.opening_host`.
- P74 must not infer missing evidence, treat missing evidence as failure, or promote preview metadata into compliance logic.

## Tasks

1. Write RED tests for opening provenance consistency.
   - Lock summary counts for semantic, measurement, host, and all-three provenance.
   - Lock Viewer/Studio data for per-opening missing provenance prompts.

2. Implement minimal deterministic coverage support.
   - Add summary counts to `layout_3d`.
   - Add Viewer adapter data under `opening_provenance_consistency`.
   - Render a compact Studio coverage note and sample table.

3. Update docs and planning.
   - Backfill P73 planning state because commit `e9b6eb3` carried code/tests but local `.planning` still ended at P72.
   - Add P74 roadmap, state, changelog, readiness, and README notes.

## Success Criteria

- Coverage counts and samples are deterministic.
- Missing semantic/measurement/host signals are labeled as review prompts only.
- No rule-engine, issue lifecycle, IFC dependency, or geometry behavior changes.
- Full validation passes before commit.
