# P55-01 Plan: Complex Benchmark Expansion

## Objective

Expand the active drawing-understanding benchmark suite with another reviewed real sheet and a deterministic complex mixed-sheet regression case, while preserving release-readiness guardrails around generated evidence.

## Context

P54 closed the handoff transfer-integrity loop. The next maturity gap is benchmark breadth: the suite should cover more real sheet inventory and more complex multi-sheet generated structures without weakening the claim boundary.

## Tasks

1. Add failing tests that require suite active count to increase and require a new mixed-sheet case to expose sheet graph and sheet issue checks.
2. Add Medfield A-2 Second Floor Plan expected inventory and committed drawing-understanding run snapshot.
3. Add deterministic generated mixed-sheet-set PDF, run artifacts, expected inventory, provenance, and suite manifest entry.
4. Update readiness docs, changelog, roadmap, state, and Notion mirror.

## Success Criteria

- Packaged suite passes with active=7, pending=0, known_gap=0.
- Release readiness remains `evidence_ready` with real_active=3 and generated_active=3.
- Generated evidence does not outnumber active real drawing evidence.
- New generated mixed-sheet case checks plan/schedule/elevation classification, per-plan sheet_graphs, skipped pages, and per-sheet preview issues.
- Full validation passes.

